1.객체란 object 존재하는 사물 혹은 개년 논리와 같은 무형적인 것들도 객라 정의
 예를 들어 사람 차 등 물리적 존내 개념적인 주문 계산이 객체가 된다.
 속성(필드) + 동작(메소드)로 구성
 자동차의 모델명 색깔 등이 필드에 해당 차가 움직이는것이 메소드 해당
 속성: 객체가 가진 고유한 특성
 동작: 객체의 행동 패턴

 2. 클래스(class)
 객체를 만들기 위한 설계도.
 클래스는 필드와 객체의 행동을 나타내는 메소드로 구성
 pubilc class 클래스이름{
 	int	property; // 속성 (멤버 필드)
 	void methodName(){ ... } // 메소드
 }
 클래스를 통해 객체를 생성방법은 : Car c1 = new Car();
 여기서 new Car();는 데이터를 동적할당하는부분이명 메모리의 힙 영역에 할당
 Car c1은 지역변수, 스택 역역에 할당이 되면 new Car();의 참조변수가 된다.

 3. 인스턴스
클래스로 객체를 만들어 하나의 객체가 생성되어 메모리에 할당된 것
public class Main {
    public static void main(String[] args) {
        Calculate cal = new Calculate();
    }
}
// Calculate 클래스에 Calculate 타입의 cal 인스턴스를 선언 및 메모리 할당

4. 속성
속성은 클래스내에 있는 변수, 곧 객체의 상태이다
